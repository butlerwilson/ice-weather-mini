<view class="nav-bar-container" style="padding-top:{{statusBarHeight}}px;">
  <view class="nav-bar" style="height: {{navHeight}}px">
    <view wx:if="{{navIcon}}" class="iconfont {{navIcon}} nav-icon" bindtap="clickNavIcon" />
    <view wx:if="{{navTitle}}">{{navTitle}}</view>
  </view>
</view>
